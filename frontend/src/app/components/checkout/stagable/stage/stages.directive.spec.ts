import { StagesDirective } from './stages.directive';

describe('StagesDirective', () => {
  it('should create an instance', () => {
    const directive = new StagesDirective();
    expect(directive).toBeTruthy();
  });
});
