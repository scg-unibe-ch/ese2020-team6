import { MapSearchValidatorDirective } from './map-search-validator.directive';

describe('MapSearchValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MapSearchValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
