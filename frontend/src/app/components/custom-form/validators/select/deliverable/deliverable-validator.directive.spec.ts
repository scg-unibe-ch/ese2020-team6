import { DeliverableValidatorDirective } from './deliverable-validator.directive';

describe('DeliverableValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DeliverableValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
