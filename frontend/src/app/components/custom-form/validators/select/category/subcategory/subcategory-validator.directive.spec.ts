import { SubcategoryValidatorDirective } from './subcategory-validator.directive';

describe('SubcategoryValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SubcategoryValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
