import { CategoryValidatorDirective } from './category-validator.directive';

describe('CategoryValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoryValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
