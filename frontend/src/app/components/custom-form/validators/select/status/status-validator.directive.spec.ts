import { StatusValidatorDirective } from './status-validator.directive';

describe('StatusValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
