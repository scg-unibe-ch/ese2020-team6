import { OfferTypeValidatorDirective } from './offer-type-validator.directive';

describe('OfferTypeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new OfferTypeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
