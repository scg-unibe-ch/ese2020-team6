import { UsernameOrEmailValidatorDirective } from './username-or-email-validator.directive';

describe('UsernameOrEmailValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameOrEmailValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
