import { RejectionMessageValidatorDirective } from './rejection-message-validator.directive';

describe('RejectionMessageValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RejectionMessageValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
