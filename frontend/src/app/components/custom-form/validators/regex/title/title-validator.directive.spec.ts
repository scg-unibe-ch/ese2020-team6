import { TitleValidatorDirective } from './title-validator.directive';

describe('TitleValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
