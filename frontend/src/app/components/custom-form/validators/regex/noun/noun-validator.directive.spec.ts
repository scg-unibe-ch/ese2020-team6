import { NounValidatorDirective } from './noun-validator.directive';

describe('NounValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NounValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
