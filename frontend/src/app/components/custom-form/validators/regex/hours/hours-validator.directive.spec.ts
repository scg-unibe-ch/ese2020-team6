import { HoursValidatorDirective } from './hours-validator.directive';

describe('HoursValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new HoursValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
