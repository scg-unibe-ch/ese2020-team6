import { OrderCardDirective } from './order-card.directive';

describe('OrderCardDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderCardDirective();
    expect(directive).toBeTruthy();
  });
});
