import { ButtonEmptyDirective } from './button-empty.directive';

describe('ButtonEmptyDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonEmptyDirective();
    expect(directive).toBeTruthy();
  });
});
