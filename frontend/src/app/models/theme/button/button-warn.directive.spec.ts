import { ButtonWarnDirective } from './button-warn.directive';

describe('ButtonWarnDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonWarnDirective();
    expect(directive).toBeTruthy();
  });
});
