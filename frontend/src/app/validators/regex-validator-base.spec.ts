import { RegexValidatorBase } from './regex-validator-base';

describe('RegexValidatorBase', () => {
  it('should create an instance', () => {
    expect(new RegexValidatorBase()).toBeTruthy();
  });
});
