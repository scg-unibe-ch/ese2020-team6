import { HouseNumberValidatorDirective } from './house-number-validator.directive';

describe('HouseNumberValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new HouseNumberValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
