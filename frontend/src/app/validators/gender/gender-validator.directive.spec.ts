import { GenderValidatorDirective } from './gender-validator.directive';

describe('GenderValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new GenderValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
