import { PlzValidatorDirective } from './plz-validator.directive';

describe('PlzValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PlzValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
